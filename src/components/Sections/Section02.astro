---
import SectionLayout from "../../layouts/SectionLayout.astro";
import ScrollArrow from "../../components/ScrollArrow.astro";

interface Props {
    maskedText?: string;
    para?: string;
    textStyle?: string;
    subtitle?: string;
}

const {
    maskedText = "Say Goodbye To Guesswork With Our Venue Diary Data",
    para = "Find out who’s available, when, & where.",
    subtitle = "",
} = Astro.props;
---

<SectionLayout
    variant="image"
    imageFolder="secret"
    imageName="pexels-cottonbro-6284254.jpg"
    className="Section03"
    contentPosition="right"
    verticalAlignment="center"
>
    <section class="h1-subtitle">
        <header>
            <h1 class="masked-text">{maskedText}</h1>
            <p class="subtitle">{subtitle}</p>
        </header>

        <p class="para-content">
            <h2 class="para">{para}</h2>
        </p>
    </section>
</SectionLayout>
<ScrollArrow />

<style lang="scss">
    .section {
        font-weight: 200;
        min-height: 100vh;
    }

    .h1-subtitle {
        display: flex;
        flex-direction: column;
        padding-left: 10%;
        padding-right: 10%;
    }

    .para-content {
        // display: flex;
        // align-self: center;
    }

    .para {
        font-size: clamp(1rem, 4vh, 8rem);
        font-weight: 200;
        // color: #292219;
        width: 100%;
        // text-align: center;
        left: 5rem;
        bottom: 3rem;
        color: #f7f1d1;
        opacity: 0.9;

        // background-clip: text;
        // -webkit-background-clip: text;
        // -webkit-text-stroke: 1px rgba(40, 40, 40, 0);
        // background-color: #f7f1d1;
        // color: transparent;
        // mix-blend-mode: overlay;
        // text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.1);
        // opacity: 0.9;
    }

    .masked-text {
        align-self: flex-start;
        width: 100%;
        font-size: clamp(2rem, 10vw, 10rem);
        font-weight: 400;
        color: white;
        margin-bottom: 8vh;
        padding-top: 5vh;
        /* Masked styling */
        // background-clip: text;
        // -webkit-background-clip: text;
        // -webkit-text-stroke: 1px rgba(40, 40, 40, 0);
        // background-color: #f7f1d1;
        // mix-blend-mode: overlay;
        // color: transparent;
        // text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.1);
    }

    .subtitle {
        text-align: end;
        min-width: 8vh;
        font-weight: 200;
        font-size: clamp(1rem, 3vh, 2rem);

        color: white;
        opacity: 0.8;

        // background-clip: text;
        // -webkit-background-clip: text;
        // -webkit-text-stroke: 1px rgba(40, 40, 40, 0);
        // background-color: black;
        // color: transparent;
        // mix-blend-mode: overlay;
    }

    @include for-size(tablet, up) {
        .h1-subtitle {
            display: flex;
            flex-direction: column;
            // align-content: flex-start;
            gap: 0.5rem;
            padding-bottom: 25vh;
            padding-left: 4vh;
            // padding-right: 4vh;
        }
    }

    @include for-size(tablet, up) {
        .masked-text {
            display: flex;

            padding: 2, 7vh, 2vh;
            font-size: clamp(4rem, 12vw, 25rem);
            max-width: 40vh;
            font-weight: 400;
            /* Masked styling */
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-stroke: 1px rgba(40, 40, 40, 0);
            background-color: #f7f1d1;
            mix-blend-mode: overlay;
            color: transparent;
            text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.1);
        }
    }

    @include for-size(tablet, up) {
        .subtitle {
            display: flex;
            align-self: flex-end;
            min-width: 8vh;
            font-weight: 400;
            font-size: clamp(1rem, 3vh, 10rem);
            color: white;
            opacity: 0.8;
        }
    }

    @include for-size(tablet, up) {
        .para {
            display: flex;
            align-self: baseline;
            font-size: clamp(1rem, 5vh, 8rem);
            font-weight: 200;
            // color: #292219;
            max-width: 80vh;
            // left: 5rem;
            // bottom: 4rem;
            color: #f7f1d1;
            opacity: 0.8;

            // background-clip: text;
            // -webkit-background-clip: text;
            // -webkit-text-stroke: 1px rgba(40, 40, 40, 0);
            // background-color: black;
            // color: transparent;
            // mix-blend-mode: overlay;
            // text-shadow: 1px 1px 2px rgba(255, 255, 255, 0.1);
            // opacity: 0.9;
        }
    }
</style>
